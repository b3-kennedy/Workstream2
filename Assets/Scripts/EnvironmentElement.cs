using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnvironmentElement : MonoBehaviour
{
    public enum ElementType {FIRE, WATER, EARTH, AIR, STEAM};
    public ElementType type;
}
